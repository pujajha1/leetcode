#https://leetcode.com/problems/valid-palindrome/
class Solution:
    def isPalindrome(self, s: str) -> bool:
        k=''
        for i in s:
            if i.isalnum():
                k+=i
        if(k.lower()==k[::-1].lower()):
            return True
        return False
        
